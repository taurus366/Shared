package org.system.shared;

public class Utils {

//DateTimeFormatter formatter = DateTimeFormatter.ofPattern(Utils.PATTERN_FORMAT);
//  entity.getCreated().atZone(zoneId).toLocalDateTime().format(formatter);
    public static final String PATTERN_FORMAT = "dd/MM/yyyy HH:mm";

}
